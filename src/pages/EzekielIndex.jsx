/**
 * EzekielIndex.jsx — /models/ezekiel: the chooser for the house Ezekiel saw
 * (Walk, guided or on foot). The generic ModelIndex on lib/models/ezekiel.js.
 */
import ModelIndex from './ModelIndex.jsx';
import MODEL from '../lib/models/ezekiel.js';

export default function EzekielIndex() { return <ModelIndex model={MODEL} />; }
