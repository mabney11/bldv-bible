/**
 * RevelationCityIndex.jsx — /models/revelation-city: the chooser for the city of the Revelation (Descend guided, or on
 * foot at its gate). The generic ModelIndex on lib/models/revelation-city.js.
 */
import ModelIndex from './ModelIndex.jsx';
import MODEL from '../lib/models/revelation-city.js';

export default function RevelationCityIndex() { return <ModelIndex model={MODEL} />; }
