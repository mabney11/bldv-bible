/**
 * EzekielCityIndex.jsx — /models/ezekiel-city: the chooser for the city and the holy portion (Portion from above,
 * City guided or on foot). The generic ModelIndex on lib/models/ezekiel-city.js.
 */
import ModelIndex from './ModelIndex.jsx';
import MODEL from '../lib/models/ezekiel-city.js';

export default function EzekielCityIndex() { return <ModelIndex model={MODEL} />; }
